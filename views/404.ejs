<!DOCTYPE html>
<html lang="en" data-theme="dim">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>404 - Page Not Found</title>
  <link href="/css/main.css" rel="stylesheet">
</head>
<body>
  <%- include('partials/navbar') %>

  <div class="container mx-auto p-10 text-center">
    <div class="hero bg-base-200 rounded-xl p-10">
      <h1 class="text-5xl font-bold text-error mb-4">404</h1>

      <!-- Dynamic error message -->
      <% if (typeof message !== "undefined") { %>
        <p class="text-lg mb-2"><%= message %></p>
      <% } else { %>
        <p class="text-lg mb-2">Oops! The page you’re looking for doesn’t exist.</p>
      <% } %>

      <a href="/" class="btn btn-primary mt-6">Back to Home</a>
    </div>
  </div>
</body>
</html>
